"""
Location Processor Lambda — consumes raw location events from SQS,
geocodes them, detects changes, and publishes enriched events to SNS.

Implemented in Phase 2.
"""


def lambda_handler(event, context):
    raise NotImplementedError("Location processor — implemented in Phase 2")
